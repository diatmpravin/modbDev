<% form_for :report, report, :url => reports_path, :html => { :class => 'multiSubmit', :method => :post } do |f| -%>
  <% report.errors.each do |error| -%>
    <div class="error"><%= error %></div>
  <% end -%>

  <div class="col">
    <div class="field">
      <%= f.label :type %>
      <%= f.select :type, vehicle_report_options %>
    </div>

    <% f.fields_for :range, report.range do |f_r| -%>
      <div class="field">
        <%= f_r.label :type, 'Date Range' %>
        <%= f_r.select :type, range_options %>
      </div>

      <div id="date_select" class="field" style="display:none">
        <%= label_tag '' %>
        <%= f_r.text_field :start, :size => 10, :maxlength => 10 %>
        &nbsp; through &nbsp;
        <%= f_r.text_field :end, :size => 10, :maxlength => 10 %>
      </div>
    <% end -%>
    
    <%= hidden_field_tag 'device_ids', '' %>
  </div>

  <div class="col description">
    <div class="report_description" id="report_0_description">
      The Vehicle Summary Report shows aggregated data for one or many vehicles
      over the selected time frame. Report data includes: Vehicle Name, Miles Driven,
      Average Fuel Economy, Operating Time, Idle Time, and number of Events for Speed,
      GeoFence, Idle Time, Aggressive Driving, and After Hours.
      <br/><br/>
      To create a report, select a Date Range, select the desired Vehicles and then click
      "Run".
    </div>

    <div class="report_description" id="report_1_description" style="display:none">
      The Daily Detail Report shows daily summary data for one vehicle over the selected time
      frame. Report data includes: Vehicle Name, Miles Driven, Average Fuel Economy, Operating
      Time, Idle Time, and number of Events for Speed, GeoFence, Idle Time, Aggressive Driving,
      and After Hours.
      <br/><br/>
      To create a report, select a Date Range, select <strong>one</strong> Vehicle and then click "Run".
    </div>

    <div class="report_description" id="report_2_description" style="display:none">
      The Fuel Economy Correlation Graph shows average fuel economy data correlated with the
      selected driver behavior variable over the selected time frame. The Graph data shows
      the vehicle fuel economy data as a line on the graph and correlation variable as bars.
      <br/><br/>
      This report works best using "This Month", "Last Month", "This Week", and "Last Week".
      <br/><br/>
      To create a graph, select a Date Range, select <strong>one</strong> Vehicle and then click "Run".
    </div>

    <div class="report_description" id="report_3_description" style="display:none">
      The Trip Detail Report shows details for each trip over the selected time frame. Report data includes:
      Start Time, End Time, Miles Driven, Average Fuel Economy, Operating
      Time, Idle Time, and number of Events for Speed, GeoFence, Idle Time, Aggressive Driving,
      and After Hours.
      <br/><br/>
      To create a report, select a Date Range, select the desired Vehicles and then click "Run".
    </div>

    <div class="report_description" id="report_4_description" style="display:none">
      The Landmark Summary Report shows arrivals and departures from landmarks. Report data includes:
      Arrival date/time, Departure date/time, stop time
      <br/><br/>
      To create a report, select a Date Range, select the desired Vehicles and then click "Run".
    </div>

    <div class="report_description" id="report_5_description" style="display:none">
      The Fuel Economy Summary Graph shows average fuel economy data for comparison of multiple
      vehicles over the selected time frame. The Graph data shows each individual vehicle as
      a line on the graph.
      <br/><br/>
      To create a graph, select a Date Range, select the desired Vehicles and then click
      "Run".
    </div>

    <div class="report_description" id="report_6_description" style="display:none">
	 	The Exception Detail Report shows details for each event over the selected time frame. Events are situations that could trigger alerts, such as Speed Alert, Aggressive Driving Alert, and After Hours Alert. Report data includes: Vehicle, Date, Time, Event, and Event Detail. (For Speed Events, the Event Detail column will include the recorded speed of the vehicle.) 
      <br/><br/>
      To create a graph, select a Date Range, select the desired Vehicles and then click
      "Run".
    </div>

    <div class="report_description" id="report_7_description" style="display:none">
      The Operating Time Summary Report shows total driving times, idle times, and percentage
      of times spent idling for vehicles
      over the selected time frame. 
      <br/><br/>
      To create a report, select a Date Range, select the desired Vehicles and then click "Run".
    </div>

    <div class="report_description" id="report_8_description" style="display:none">
      The Fuel Economy Summary Graph shows average fuel economy data for comparison of multiple
      vehicles over the selected time frame. The Graph data shows each individual vehicle as
      a line on the graph.
      <br/><br/>
      To create a graph, select a Date Range, select the desired Vehicles and then click "Run".
    </div>

  </div>

  <div class="clear"></div>
  <div class="buttons">
    <div class="loading"></div>
    <!-- <%= submit_tag 'Run Report' %> -->
    <!-- <%= button_tag 'Cancel', :class => 'cancel' %> -->
  </div>
  <div class="clear"></div>
<% end -%>
