<% @invoices.each do |invoice| -%>
  <li>
    <div class="row clear">
      <div class="listing">
        <span class="number"><%= h(invoice.name) %></span>
      </div>
      <div class="invoice_data">
        <span class="date_generated"><%= h(invoice.generated_on) %></span>
        <span class="date_due"><%= h(invoice.due_on) %></span>
        <span class="amount"><%= h(invoice.amount) %></span>
        <span class="<%= invoice.paid? ? 'paid' : 'unpaid' %>"><span>
      </div>
    </div>
  </li>
<% end -%>
