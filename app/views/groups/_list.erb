<% new_tree(current_user.device_group_or_root, :include_parent => true, :root_ol => false, :vehicles => false) do |group, level| -%>
  <div class="row group clear" id="<%= dom_id(group) %>">
    <div class="listing">
      <span class="indent"></span>
      <span class="collapsible open"></span>
      <span class="name"><%= group.name %></span>
      <span class="checkbox"></span>
    </div>
  </div>
<% end -%>
