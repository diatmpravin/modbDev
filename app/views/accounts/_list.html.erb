<%= paginate accounts %>

<table class="data" style="width:100%">
  <thead>
    <tr>
      <th>Number</th>
      <th>Name</th>
      <% if account.can_assign_reseller? -%>
        <th>Reseller?</th>
        <th>Can Create Resellers?</th>
      <% end -%>
      <th></th>
    <tr>
  </thead>
  <% accounts.each do |a| -%>
    <tr class="<%=cycle('','odd')%>">
      <td><%= h a.number %></td>
      <td><%= h a.name %></td>
      <% if account.can_assign_reseller? -%>
        <td><%= h a.reseller? %></td>
        <td><%= h a.can_assign_reseller? %></td>
      <% end -%>
      <% if account.reseller? -%>
        <td class="buttons">
          <% requires_role(User::Role::ADMIN) do -%>
            <%= link_to 'Edit', edit_account_path(account), :class => 'edit', :title => 'Edit' %>
            <!-- <%= link_to 'Delete', account_path(account), :class => 'delete', :title => 'Remove' %> -->
          <% end -%>
        </td>
      <% end -%>
    </tr>  
  <% end -%>
</table>

<%= paginate accounts %>