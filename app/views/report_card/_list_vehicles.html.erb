<%= render :partial => "entry", :collection => report_card.data, :as => :report, 
  :locals => {:group => report_card.group, :vehicle => true, :days => (report_card.end - report_card.start).to_i + 1}-%>
